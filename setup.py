#!/usr/bin/env python

from distutils.core import setup
import setuptools

setup(
    name="micro",
    version="0.1",
    description="Code used to quantify micro-topographical changes on gravel forest roads",
    packages=setuptools.find_packages(),
)